package io.github.dynamictreestfc.util;

import java.util.Random;
import com.ferreusveritas.dynamictrees.api.worldgen.BiomePropertySelectors;
import net.minecraft.core.BlockPos;
import net.minecraft.world.level.block.state.BlockState;

public class TFCSpeciesSelector implements BiomePropertySelectors.SpeciesSelector
{
    // todo implement

    @Override
    public BiomePropertySelectors.SpeciesSelection getSpecies(BlockPos pos, BlockState state, Random random)
    {
        return null;
    }
}
